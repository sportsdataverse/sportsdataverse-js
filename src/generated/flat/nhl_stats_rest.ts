// AUTO-GENERATED by tools/codegen/generate.mjs — do not edit by hand.
// Run `npm run codegen` to regenerate from tools/codegen/endpoints/*.yaml.
//
// WRITTEN flat-API module for NHL Stats REST (api stem `nhl_stats_rest`). Each export
// delegates to the shared `callFlat(def, params)` core, so TypeScript /
// TypeDoc / IDEs see every wrapper.

import { callFlat } from "../../leagues/_make_flat.js";
import type { WrapperDef, WrapperFn } from "../../core/types.js";

const COMPONENT_SEASON_DEF: WrapperDef = {
  "short": "component_season",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/componentSeason",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": []
};

/**
 * NHL Stats REST — component season.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/componentSeason`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - accepted for symmetry, but this endpoint has no registered parser, so the raw response is always returned.
 * @returns The raw response (this endpoint has no parser).
 * @example await sdv.nhl.nhlStatsRestComponentSeason({});
 */
export const nhlStatsRestComponentSeason: WrapperFn = (params = {}) => callFlat(COMPONENT_SEASON_DEF, params);
/** snake_case alias of {@link nhlStatsRestComponentSeason} (py/R parity). */
export const nhl_stats_rest_component_season = nhlStatsRestComponentSeason;

const CONFIG_DEF: WrapperDef = {
  "short": "config",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/config",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": []
};

/**
 * NHL Stats REST — config.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/config`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - accepted for symmetry, but this endpoint has no registered parser, so the raw response is always returned.
 * @returns The raw response (this endpoint has no parser).
 * @example await sdv.nhl.nhlStatsRestConfig({});
 */
export const nhlStatsRestConfig: WrapperFn = (params = {}) => callFlat(CONFIG_DEF, params);
/** snake_case alias of {@link nhlStatsRestConfig} (py/R parity). */
export const nhl_stats_rest_config = nhlStatsRestConfig;

const CONTENT_MODULE_DEF: WrapperDef = {
  "short": "content_module",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/content/module/{template_key}",
  "pathParams": [
    {
      "name": "template_key"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": []
};

/**
 * NHL Stats REST — content module.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/content/module/{template_key}`
 *
 * @param params.template_key - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - accepted for symmetry, but this endpoint has no registered parser, so the raw response is always returned.
 * @returns The raw response (this endpoint has no parser).
 * @example await sdv.nhl.nhlStatsRestContentModule({ template_key: '…' });
 */
export const nhlStatsRestContentModule: WrapperFn = (params = {}) => callFlat(CONTENT_MODULE_DEF, params);
/** snake_case alias of {@link nhlStatsRestContentModule} (py/R parity). */
export const nhl_stats_rest_content_module = nhlStatsRestContentModule;

const COUNTRY_DEF: WrapperDef = {
  "short": "country",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/country",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/country"
};

/**
 * NHL Stats REST — country.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/country`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestCountry({});
 */
export const nhlStatsRestCountry: WrapperFn = (params = {}) => callFlat(COUNTRY_DEF, params);
/** snake_case alias of {@link nhlStatsRestCountry} (py/R parity). */
export const nhl_stats_rest_country = nhlStatsRestCountry;

const DRAFT_DEF: WrapperDef = {
  "short": "draft",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/draft",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — draft.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/draft`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestDraft({});
 */
export const nhlStatsRestDraft: WrapperFn = (params = {}) => callFlat(DRAFT_DEF, params);
/** snake_case alias of {@link nhlStatsRestDraft} (py/R parity). */
export const nhl_stats_rest_draft = nhlStatsRestDraft;

const FRANCHISE_DEF: WrapperDef = {
  "short": "franchise",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/franchise",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/franchise"
};

/**
 * NHL Stats REST — franchise.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/franchise`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestFranchise({});
 */
export const nhlStatsRestFranchise: WrapperFn = (params = {}) => callFlat(FRANCHISE_DEF, params);
/** snake_case alias of {@link nhlStatsRestFranchise} (py/R parity). */
export const nhl_stats_rest_franchise = nhlStatsRestFranchise;

const GAME_DEF: WrapperDef = {
  "short": "game",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/game",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — game.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/game`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestGame({});
 */
export const nhlStatsRestGame: WrapperFn = (params = {}) => callFlat(GAME_DEF, params);
/** snake_case alias of {@link nhlStatsRestGame} (py/R parity). */
export const nhl_stats_rest_game = nhlStatsRestGame;

const GLOSSARY_DEF: WrapperDef = {
  "short": "glossary",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/glossary",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/glossary"
};

/**
 * NHL Stats REST — glossary.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/glossary`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestGlossary({});
 */
export const nhlStatsRestGlossary: WrapperFn = (params = {}) => callFlat(GLOSSARY_DEF, params);
/** snake_case alias of {@link nhlStatsRestGlossary} (py/R parity). */
export const nhl_stats_rest_glossary = nhlStatsRestGlossary;

const GOALIE_REPORT_DEF: WrapperDef = {
  "short": "goalie_report",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/goalie/{report}",
  "pathParams": [
    {
      "name": "report"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/goalie_report"
};

/**
 * NHL Stats REST — goalie report.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/goalie/{report}`
 *
 * @param params.report - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestGoalieReport({ report: '…' });
 */
export const nhlStatsRestGoalieReport: WrapperFn = (params = {}) => callFlat(GOALIE_REPORT_DEF, params);
/** snake_case alias of {@link nhlStatsRestGoalieReport} (py/R parity). */
export const nhl_stats_rest_goalie_report = nhlStatsRestGoalieReport;

const LEADERS_GOALIES_DEF: WrapperDef = {
  "short": "leaders_goalies",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/leaders/goalies/{attribute}",
  "pathParams": [
    {
      "name": "attribute"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — leaders goalies.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/leaders/goalies/{attribute}`
 *
 * @param params.attribute - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestLeadersGoalies({ attribute: '…' });
 */
export const nhlStatsRestLeadersGoalies: WrapperFn = (params = {}) => callFlat(LEADERS_GOALIES_DEF, params);
/** snake_case alias of {@link nhlStatsRestLeadersGoalies} (py/R parity). */
export const nhl_stats_rest_leaders_goalies = nhlStatsRestLeadersGoalies;

const LEADERS_SKATERS_DEF: WrapperDef = {
  "short": "leaders_skaters",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/leaders/skaters/{attribute}",
  "pathParams": [
    {
      "name": "attribute"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — leaders skaters.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/leaders/skaters/{attribute}`
 *
 * @param params.attribute - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestLeadersSkaters({ attribute: '…' });
 */
export const nhlStatsRestLeadersSkaters: WrapperFn = (params = {}) => callFlat(LEADERS_SKATERS_DEF, params);
/** snake_case alias of {@link nhlStatsRestLeadersSkaters} (py/R parity). */
export const nhl_stats_rest_leaders_skaters = nhlStatsRestLeadersSkaters;

const MILESTONES_GOALIES_DEF: WrapperDef = {
  "short": "milestones_goalies",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/milestones/goalies",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — milestones goalies.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/milestones/goalies`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestMilestonesGoalies({});
 */
export const nhlStatsRestMilestonesGoalies: WrapperFn = (params = {}) => callFlat(MILESTONES_GOALIES_DEF, params);
/** snake_case alias of {@link nhlStatsRestMilestonesGoalies} (py/R parity). */
export const nhl_stats_rest_milestones_goalies = nhlStatsRestMilestonesGoalies;

const MILESTONES_SKATERS_DEF: WrapperDef = {
  "short": "milestones_skaters",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/milestones/skaters",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — milestones skaters.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/milestones/skaters`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestMilestonesSkaters({});
 */
export const nhlStatsRestMilestonesSkaters: WrapperFn = (params = {}) => callFlat(MILESTONES_SKATERS_DEF, params);
/** snake_case alias of {@link nhlStatsRestMilestonesSkaters} (py/R parity). */
export const nhl_stats_rest_milestones_skaters = nhlStatsRestMilestonesSkaters;

const PING_DEF: WrapperDef = {
  "short": "ping",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/ping",
  "pathParams": [],
  "queryParams": []
};

/**
 * NHL Stats REST — ping.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/ping`
 *
 * @param params.parsed - accepted for symmetry, but this endpoint has no registered parser, so the raw response is always returned.
 * @returns The raw response (this endpoint has no parser).
 * @example await sdv.nhl.nhlStatsRestPing({});
 */
export const nhlStatsRestPing: WrapperFn = (params = {}) => callFlat(PING_DEF, params);
/** snake_case alias of {@link nhlStatsRestPing} (py/R parity). */
export const nhl_stats_rest_ping = nhlStatsRestPing;

const PLAYERS_DEF: WrapperDef = {
  "short": "players",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/players",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — players.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/players`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestPlayers({});
 */
export const nhlStatsRestPlayers: WrapperFn = (params = {}) => callFlat(PLAYERS_DEF, params);
/** snake_case alias of {@link nhlStatsRestPlayers} (py/R parity). */
export const nhl_stats_rest_players = nhlStatsRestPlayers;

const SEASON_DEF: WrapperDef = {
  "short": "season",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/season",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/season"
};

/**
 * NHL Stats REST — season.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/season`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestSeason({});
 */
export const nhlStatsRestSeason: WrapperFn = (params = {}) => callFlat(SEASON_DEF, params);
/** snake_case alias of {@link nhlStatsRestSeason} (py/R parity). */
export const nhl_stats_rest_season = nhlStatsRestSeason;

const SHIFTCHARTS_DEF: WrapperDef = {
  "short": "shiftcharts",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/shiftcharts",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — shiftcharts.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/shiftcharts`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestShiftcharts({});
 */
export const nhlStatsRestShiftcharts: WrapperFn = (params = {}) => callFlat(SHIFTCHARTS_DEF, params);
/** snake_case alias of {@link nhlStatsRestShiftcharts} (py/R parity). */
export const nhl_stats_rest_shiftcharts = nhlStatsRestShiftcharts;

const SKATER_REPORT_DEF: WrapperDef = {
  "short": "skater_report",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/skater/{report}",
  "pathParams": [
    {
      "name": "report"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/skater_report"
};

/**
 * NHL Stats REST — skater report.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/skater/{report}`
 *
 * @param params.report - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestSkaterReport({ report: '…' });
 */
export const nhlStatsRestSkaterReport: WrapperFn = (params = {}) => callFlat(SKATER_REPORT_DEF, params);
/** snake_case alias of {@link nhlStatsRestSkaterReport} (py/R parity). */
export const nhl_stats_rest_skater_report = nhlStatsRestSkaterReport;

const TEAM_DEF: WrapperDef = {
  "short": "team",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/team",
  "pathParams": [
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — team.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/team`
 *
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestTeam({});
 */
export const nhlStatsRestTeam: WrapperFn = (params = {}) => callFlat(TEAM_DEF, params);
/** snake_case alias of {@link nhlStatsRestTeam} (py/R parity). */
export const nhl_stats_rest_team = nhlStatsRestTeam;

const TEAM_BY_ID_DEF: WrapperDef = {
  "short": "team_by_id",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/team/id/{team_id}",
  "pathParams": [
    {
      "name": "team_id"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest"
};

/**
 * NHL Stats REST — team by id.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/team/id/{team_id}`
 *
 * @param params.team_id - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestTeamById({ team_id: '…' });
 */
export const nhlStatsRestTeamById: WrapperFn = (params = {}) => callFlat(TEAM_BY_ID_DEF, params);
/** snake_case alias of {@link nhlStatsRestTeamById} (py/R parity). */
export const nhl_stats_rest_team_by_id = nhlStatsRestTeamById;

const TEAM_REPORT_DEF: WrapperDef = {
  "short": "team_report",
  "flat": true,
  "api": "nhl_stats_rest",
  "host": "https://api.nhle.com/stats/rest",
  "scope": "universal",
  "path": "/{lang}/team/{report}",
  "pathParams": [
    {
      "name": "report"
    },
    {
      "name": "lang",
      "required": false,
      "default": "en"
    }
  ],
  "queryParams": [],
  "parser": "parse_nhl_stats_rest",
  "returnsSchema": "native/nhl_stats_rest/team_report"
};

/**
 * NHL Stats REST — team report.
 *
 * **Endpoint:** `GET https://api.nhle.com/stats/rest/{lang}/team/{report}`
 *
 * @param params.report - path parameter.
 * @param params.lang - path parameter *(optional)*.
 * @param params.parsed - when `true`, route the payload through this endpoint's parser and return tidy rows instead of the raw response.
 * @returns The raw response by default; a tidy array of row objects when `{ parsed: true }`.
 * @example await sdv.nhl.nhlStatsRestTeamReport({ report: '…' });
 */
export const nhlStatsRestTeamReport: WrapperFn = (params = {}) => callFlat(TEAM_REPORT_DEF, params);
/** snake_case alias of {@link nhlStatsRestTeamReport} (py/R parity). */
export const nhl_stats_rest_team_report = nhlStatsRestTeamReport;

node_modules
.vscode
examples
mochawesome-report
dist/
# Local-only TypeDoc HTML (`npm run docs`). The DEPLOYED API reference is
# generated into the Docusaurus site by docusaurus-plugin-typedoc instead.
typedoc-build/
# docs/api/ is the docs site's Vercel serverless functions dir (playground proxy).

# Yahoo Sports shangrila — generated returns schema.
# Parsed by parse_yahoo_list. The spec ships no typed list item for this query, so columns are not enumerable ahead of a live capture; the parser snake_cases + flattens whatever keys the payload ships.
schema: playbook_golf_tournament
kind: dataframe
columns: []

# Yahoo Sports shangrila — generated returns schema.
# Parsed by parse_yahoo_shangrila_list. Columns derived from the spec's typed root-list item props (deep-flattened with `_`, snake_cased to match the parser output).
schema: team_roster
kind: dataframe
columns:
- name: league_current_season
  type: character
- name: roster
  type: list

# Fox Sports Bifrost — generated returns schema. Columns are derived from the
# spec's captured `components.schemas` (the item props of the list the parser
# flattens), snake_cased to match the parser's output. Untyped (`object`)
# responses carry `columns: []` (the parser flattens whatever the payload ships).
schema: league_teamnav
kind: dataframe
columns:
- name: title
  type: character
  description: Bifrost league_teamnav field `title`.
- name: logo_url
  type: character
  description: Bifrost league_teamnav field `logoUrl`.
- name: image_type
  type: character
  description: Bifrost league_teamnav field `imageType`.
- name: image_alt_text
  type: character
  description: Bifrost league_teamnav field `imageAltText`.
- name: entity_link
  type: list
  description: Bifrost league_teamnav field `entityLink`.
- name: template
  type: character
  description: Bifrost league_teamnav field `template`.
- name: alternate_logo_url
  type: character
  description: Bifrost league_teamnav field `alternateLogoUrl`.

# Yahoo Sports editorial — generated returns schema.
# Parsed by parse_yahoo_scores_boxscore. The editorial boxscore payload is a keyed map (id -> object) with no typed item schema; the parser unrolls + snake_cases whatever keys the payload ships, so columns are not enumerable ahead of a live capture.
schema: boxscore
kind: dataframe
columns: []

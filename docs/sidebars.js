/**
 * The sidebar is generated from the docs/ folder structure. New reference,
 * tutorial, and API pages surface automatically — no manual edits required.
 * Ordering within a folder is controlled by each doc's `sidebar_position`
 * frontmatter and by `_category_.json` files.
 */
module.exports = {
  docs: [{ type: 'autogenerated', dirName: '.' }],
};
